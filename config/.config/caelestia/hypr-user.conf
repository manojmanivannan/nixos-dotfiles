# WF-10 placeholder. Caelestia reads ~/.config/caelestia/hypr-user.conf
# for shell-specific Hyprland tweaks (e.g. VRR disable for flicker) that
# the shell wants on top of the user's own hypr/ config. It is NOT a
# replacement for config/.config/hypr/. Empty for now; WF-11/WF-12 add
# shell-specific Hyprland settings here if the live session needs them
# (see docs/wayfinder/research/nix-integration.md §4d).